# vue3中使用echarts遇到的问题

## 1、柱状图初始化有问题，鼠标移动到图上，不显示X轴和Y轴的值

原因；定义的echarts init值不能用ref等初始化，只能用Null，不然会和vue3冲突，引起echarts初始化报错

解决办法：定义值为null

## 2、折线图值和图像对不上

原因：series里不同数据的stack的值相同，折线图初始化后就会出错

经济办法：series里不同数据的stack的值设置不同
